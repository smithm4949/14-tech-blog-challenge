{{#each post as |post| }}
  <div class="post-wrapper">
    <h2>{{post.title}}</h2>
    <h4>Posted by {{user.name}} on {{post.createdAt}}</h4>
  </div>
  <div class="post-contents">
    <p>
      {{post.contents}}
    </p>
  </div>
{{/each}}